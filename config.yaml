# Configuration file for Dagster Finance Pipeline

# Transaction Classifier Model Configuration
model:
  # Minimum confidence to show a predicted category (below = UNCERTAIN)
  confidence_threshold: 0.10
  # Predictions between confidence_threshold and this value show a "Low confidence" tag in the UI
  low_confidence_threshold: 0.35
