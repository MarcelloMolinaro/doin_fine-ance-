# Python / virtual env
__pycache__/
*.py[cod]
*.pyo
.python-version
.venv/
venv/
ENV/
*.egg-info/
.eggs/

# IDE / OS noise
.DS_Store
.idea/
.vscode/

# Dagster runtime state
dagster/storage/
dagster/history/
dagster/logs/
dagster/schedules/*.db
# ML model artifacts
dagster/models/
*.pkl
*.joblib

# dbt build artifacts
dbt/target/
dbt/logs/
dbt/.user.yml

# Test / coverage caches
.pytest_cache/
.mypy_cache/
.coverage

# Dagster local state
.local_defs_state/

# Logs / runtime outputs (optional but common)
*.log

# Environment variables and secrets
.env
.env.local
docker-compose.override.yml

# Configuration files
# config.yaml

# dbt seeds - actual data files (example files are checked in)
dbt/seeds/historic_transactions.csv
dbt/seeds/seed_account_mapping_simplefin.csv
dbt/seeds/seed_account_mapping_historic.csv
dbt/seeds/seed_transaction_exclusions.csv

# Jupyter notebook checkpoints
.ipynb_checkpoints/
*/.ipynb_checkpoints/

# Database backups (saved to local server storage)
# Keep backups/ directory via .gitkeep; ignore backup files
backups/*.dump
backups/schedule.json

